DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=historical_data

# JWT auth - generate your own with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
